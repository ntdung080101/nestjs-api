export enum Role {
  ADMIN,
  STAFF,
  MANAGER,
  CUSTOMER,
}

export enum LoginRules {
  ADMIN,
  STAFF,
  MANAGER,
  CUSTOMER,
}

export enum AccountRules {
  STAFF,
  CUSTOMER,
}

export enum StaffRules {
  ADMIN,
  STAFF,
  MANAGER,
}

export enum StaffPermission {
  ADMIN,
  STAFF,
}
